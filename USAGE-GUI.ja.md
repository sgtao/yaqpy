###### [toREADME](./README.ja.md)
###### [toUSAGE](./USAGE.ja.md)
# yaqpy GUI 使い方ガイド

YAML / JSON のファイルを開いて、**必要な部分だけを取り出したり、形を変えたり**できるデスクトップアプリの使い方です。コマンドラインが苦手でも、画面を見ながら試せます。

- **読む人**：プログラミングやコマンドの経験が少ない方を想定しています
- **かかる時間**：3 章（最初の 5 分）だけなら 5 分、4 章（式の書き方）まで読んで 30 分ほど
- **この文書の例**：すべて `examples/sample.yaml` で**実際に実行して確かめた**ものです。同じ結果になります

## 目次

1. [起動する](#1-起動する)
2. [画面の見取り図](#2-画面の見取り図)
3. [最初の 5 分：開く → 選ぶ → 保存する](#3-最初の-5-分開く--選ぶ--保存する)
4. [式の書き方（初心者向け）](#4-式の書き方初心者向け)
5. [プロパティのプルダウンを使いこなす](#5-プロパティのプルダウンを使いこなす)
6. [入力形式・出力形式・見た目の設定](#6-入力形式出力形式見た目の設定)
7. [ファイルの開き方（貼り付けも使えます）](#7-ファイルの開き方貼り付けも使えます)
8. [保存とコピー](#8-保存とコピー)
9. [設定画面](#9-設定画面)
10. [エラーの読み方](#10-エラーの読み方)
11. [大きなファイルと「中止」](#11-大きなファイルと中止)
12. [困ったとき（Q&A）](#12-困ったときqa)
13. [コマンド（CLI）との対応](#13-コマンドcliとの対応)
14. [参考資料と、この文書の確かめ方](#14-参考資料とこの文書の確かめ方)
15. [Web 版（ブラウザで使う）](#15-web-版ブラウザで使う)
16. [実行ログとログ画面（v0.7.0）](#16-実行ログとログ画面v070)

---
[toTop](#toreadme)
## 1. 起動する

**PyPI から入れる場合**（GitHub のリリースから入れる方法などは [README のインストール](README.ja.md#インストール)）

```bash
pip install "yaqpy[gui]"          # または: uv tool install "yaqpy[gui]" --with flet-desktop
yaqpy-gui                 # 専用コマンドで起動
yaqpy --gui               # こちらでも同じ画面が開きます
```

すでに GUI なしで入れている場合は、部品（flet）だけを足すこともできます：`pip install "flet>=1.0,<2"`。

> **`uv tool install` / `uvx` で入れる場合は `--with flet-desktop` を付けてください。** 付けずに入れると、`yaqpy-gui` の初回起動時に Flet が `flet-desktop`（窓を描画する部品）を自動で入れようとしますが、その先が `yaqpy` 用の隔離環境とは別の場所（実行時のカレントディレクトリ付近の無関係な `.venv` など）になることがあります。この場合「OK」と表示されたのに `ModuleNotFoundError: No module named 'flet_desktop'` で起動に失敗します（[12 章](#12-困ったときqa)）。

**リポジトリを clone した場合**

```bash
uv sync --extra gui       # GUI を使うときだけ、部品（flet）が入ります
uv run yaqpy-gui          # 専用コマンドで起動
uv run yaqpy --gui        # こちらでも同じ画面が開きます
```

- **初回だけ**、画面の部品（Flet のデスクトップクライアント）の準備が入って、少し待ちます。2 回目からは、この準備は入りません
- 部品（flet）を入れていない状態で起動すると、入れ方を案内して終了します（コマンドは壊れません）
- `yaqpy --gui a.yaml` のように**実在するファイルを 1 つだけ**続けて書くと、そのファイルを開いた状態で起動します（v0.5.0 から）。式（`yaqpy --gui '.a'`）や、存在しないパス、2 つ以上のファイルは「一緒には使えません」と断られます（打ち間違いの式をファイルと見なさないための安全策です）
- 窓を閉じると、アプリは終了します（**Windows で実機確認済み**。v0.5.0 で macOS / Linux でも同じ仕組み（`page.window.on_event`）を使うようにしましたが、**実機（macOS / Linux）ではまだ確認していません**。Windows 特有の後始末（`taskkill`）は Windows でだけ行います）。下の切り替えバーの **`[⏻]`** でも終了できます。押すと「アプリを閉じますか？」と確認が出て、`[閉じる]` を選ぶと窓の × と同じ手順で終了します（v0.5.0）
- 表示言語は既定で日本語です。[設定画面](#9-設定画面)で英語に切り替えられます（v0.5.0。次の起動から反映）
- 同じ画面を**ブラウザで**使うこともできます（`yaqpy-web` または `yaqpy --web`。v0.6.0 から）。入れ方と違いは [15 章](#15-web-版ブラウザで使う)を参照してください

---
[toTop](#toreadme)
## 2. 画面の見取り図

```text
┌──────────────────────────────────────────────────────────────────────┐
│ sample.yaml (233 B)  [＋ファイルを追加]                  [閉じる]     │ ① ファイル
│ 入力形式 [auto]  出力形式 [yaml]  インデント[－  2  ＋]              │ ② 形式
│ プロパティ [.server.port = 8080            ▼]     [+ パイプを追加]   │ ③ 候補
│ 式 [.server.port          ] [📋][📄][✕][📂][💾] [▶ 実行] [■ 中止]   │ ④ 式
├───────────────────────────────┬──────────────────────────────────────┤
│ オリジナル                     │ 変換結果                [📋] [保存]   │
│ # サーバー設定                 │ 8080                                 │ ⑤ 2 つの窓
│ server:                       │                                      │
│   port: 8080 # 開発用          │                                      │
├───────────────────────────────┴──────────────────────────────────────┤
│ ✔ 1 document / 0.9 ms / 出力 5 bytes                    yaml → yaml   │ ⑥ 状態バー
├──────────────────────────────────────────────────────────────────────┤
│ [メイン] [設定] [AIに相談] [ログ]                              [⏻]     │ ⑦ 切り替え・終了
└──────────────────────────────────────────────────────────────────────┘
```

| 番号 | 場所 | 役割 |
|---|---|---|
| ① | ファイルバー | `[＋ファイルを追加]` で開く／増やす、`[閉じる]` ですべて閉じる（v0.5.0 から 1 つのボタンに統一し、ファイル名の右へ配置。[7 章](#7-ファイルの開き方貼り付けも使えます)）。開いたファイルの名前と大きさが**太字**で出ます（貼り付けたときは「（貼り付けたテキスト）」） |
| ② | 形式バー | 読み込む形式・書き出す形式（**既定は `yaml`**）・字下げの幅（数字欄への直接入力のほか `－`/`＋` でも変えられます）（[6 章](#6-入力形式出力形式見た目の設定)） |
| ③ | プロパティ | 開いた文書の「項目の一覧」から選べます（[5 章](#5-プロパティのプルダウンを使いこなす)）。右の `[+ パイプを追加]` は、式の末尾に ` | ` を足します |
| ④ | 式 | **何を取り出すか**を書く欄です。この文書の主役です（[4 章](#4-式の書き方初心者向け)）。右のアイコンは、左から **貼り付け・コピー・クリア**（v0.7.0）と、式のファイル（`.yaqpy`）の**読み込み・保存**（[8-4](#8-4-式をファイルに保存読み込みyaqpy)）。AI への相談は、下の **`[AIに相談]`** タブ（[4-13](#4-13-式が難しいときai-に相談する)）です |
| ⑤ | 2 つの窓 | 左が**原文**、右が**式を通した結果**。**左は v0.5.0 から編集もできます**（[7-2](#7-2-原文を書き換える追加編集)）。右の結果だけ保存・コピーできます。見出しの横の小さな札に、**いま採っている形式**（`xml` `toon` など）が出ます。形式バーが `auto` でも指定でも、実際に使う形式名です |
| ⑥ | 状態バー | 成功なら件数と時間、失敗なら理由が出ます。右端に「読み込んだ形式 → 書き出した形式」 |
| ⑦ | 切り替え・終了 | メイン・[設定](#9-設定画面)・[AI に相談](#4-13-式が難しいときai-に相談する)・[ログ](#16-実行ログとログ画面v070)（**ログはデスクトップ版だけ**）の画面を行き来します。`[⏻]` はアプリの終了です（v0.5.0。押すと「アプリを閉じますか？」の確認が出ます） |

> **開いたファイルそのもの（ディスク上）は、あなたが「上書きする」と答えない限り書き換わりません。** 左の窓は編集できますが（v0.5.0）、それは画面の上の入力を書き換えるだけで、保存するまで元のファイルには影響しません。

---
[toTop](#toreadme)
## 3. 最初の 5 分：開く → 選ぶ → 保存する

このリポジトリの `examples/sample.yaml` を使います。中身はこうです（コメント付きの設定ファイルです）。

```yaml
# サーバー設定
server:
  port: 8080 # 開発用
  hosts: [a, b]
  tls: &tls
    enabled: true
    cert: /etc/cert.pem
backup:
  <<: *tls
  schedule: "0 3 * * *"
items:
  - name: pen
    price: 120
  - name: book
    price: 980
```

**① 開く**：`[＋ファイルを追加]` を押して `sample.yaml` を選びます。左に原文、右に結果が出ます。式が `.`（＝全体）なので、左右はほぼ同じ内容です。コメント（`# 開発用`）も消えずに残ります。

**② プロパティから選ぶ**：`プロパティ` 欄をクリックして `port` と打つと、候補が `.server.port  = 8080` の 1 つに絞られます。それを選ぶと、式欄が `.server.port` に変わり、右の窓が `8080` になります。

**③ 式を自分で書く**：式欄を `.items[] | select(.price > 500)` に書き換えて **Enter** を押します。右の窓に、500 より高い商品だけが出ます。

```yaml
name: book
price: 980
```

**④ 形式を変える**：`出力形式` を `json` にすると、右の窓が JSON になります（左は変わりません）。

**⑤ 保存する**：右上の `[保存]` を押して、保存先を決めます。ファイル名の初期値は `sample.json`（出力形式に合わせた拡張子）です。保存した内容は、右の窓と**同じ**です。

これだけで基本は使えます。式が思いどおりに書けないときは、次の 4 章を読んでください。

---
[toTop](#toreadme)
## 4. 式の書き方（初心者向け）

**式**は「この文書のどこを、どう取り出す（変える）か」を短く書いたものです。yaqpy の式は、YAML / JSON を扱うツール `yq`（Go 版）の式を手本にしています（実装済みの演算子だけが使えます。[4-10](#4-10-まだ使えないもの)）。同じ系統の `jq`（JSON 用）の解説記事にある考え方（`.` で全体、`.キー` で中身、`|` でつなぐ、など）がそのまま通用します。

> **前提**：この章の例は、3 章の `sample.yaml` を開いた状態で式欄に打つものです。実行は **Enter**（または `[▶ 実行]`）。結果は右の窓に出ます。出力形式は既定の `yaml`（YAML 入力なら入力と同じ）のままとします。

### 4-1. まず `.`（ドット）だけ

`.` は「**いま見ている文書の全体**」です。何も絞り込まないので、開いた直後（式が `.`）は全体がそのまま出ます。式欄を**空にして実行しても `.` と同じ**です。

### 4-2. キーをたどる

`.` のあとにキーを書くと、その中身が取り出せます。階層は `.` でつなぎます。

| 式 | 意味 | 右の窓の結果 |
|---|---|---|
| `.server.port` | `server` の中の `port` | `8080` |
| `.server.tls.cert` | 3 段たどる | `/etc/cert.pem` |
| `.server.hosts` | リストごと | `[a, b]` |
| `.a.b.c` | **存在しない**キー | `null` |

存在しないキーをたどっても**エラーにはならず `null`（空）**になります。書き間違いに気づきにくいので、プロパティのプルダウン（[5 章](#5-プロパティのプルダウンを使いこなす)）で選ぶと安心です。

キー名に**空白や記号**があるときは、`.["キー名"]` と書きます（`."キー名"` は空白入りだと使えません）。

```text
.["server"]["port"]   →  8080
.["my key"]           →  （キー「my key」の値。プルダウンが自動でこの形にしてくれます）
```

### 4-3. リスト（配列）の要素：`[ ]`

リストの中身は `[ ]` で取り出します。**番号は 0 から**数えます。

| 式 | 意味 | 結果 |
|---|---|---|
| `.server.hosts[0]` | 最初の要素 | `a` |
| `.server.hosts[-1]` | **最後**の要素 | `b` |
| `.items[0]` | 商品の 1 つ目 | `name: pen` と `price: 120` |
| `.items[1].price` | 2 つ目の価格 | `980` |
| `.items[1:2]` | 範囲（1 番目から 2 番目の手前まで） | `- name: book` `price: 980` |
| `.server.hosts[]` | **全部を順に**（番号を書かない） | `a` と `b`（2 行） |
| `.items[].name` | 全商品の名前 | `pen` と `book`（2 行） |
| `.items[5]` | 範囲外 | `null` |

**`[]`（中身を空にする）が大事です。** 「リストの全要素に対して、続きの式を適用する」という意味になります。`.items[].name` は「各商品の名前」です。

### 4-4. `|`（パイプ）：結果を次の式に渡す

`|` は、**左の結果を右の式の入力にする**つなぎ役です。シェルのパイプと同じ考え方で、長い処理を小さな部品に分けて書けます。

| 式 | 読み方 | 結果 |
|---|---|---|
| `.items[] \| .name` | 各商品を取り出して、その名前を | `pen` `book` |
| `.server \| keys` | `server` の中の**キーの一覧** | `- port` `- hosts` `- tls` |
| `.items \| length` | `items` の**個数** | `2` |
| `.server.hosts \| length` | リストの長さ | `2` |

`,`（カンマ）は「**2 つの式を並べて、結果を続けて出す**」ものです。

```text
.server.port, .items[0].name   →  8080  と  pen
```

### 4-5. 絞り込む：`select( 条件 )`

`select(条件)` は「**条件に合うものだけ残す**」式です。`.items[]`（全商品）と `|` でつなぐのが定番です。

| やりたいこと | 式 | 結果 |
|---|---|---|
| 500 より高い | `.items[] \| select(.price > 500)` | `name: book` `price: 980` |
| 名前が pen | `.items[] \| select(.name == "pen")` | `name: pen` `price: 120` |
| pen 以外 | `.items[] \| select(.name != "pen")` | `name: book` `price: 980` |
| 100 以上かつ 500 未満 | `.items[] \| select(.price >= 100 and .price < 500)` | `name: pen` `price: 120` |
| 高い、または pen | `.items[] \| select(.price > 500 or .name == "pen") \| .name` | `pen` `book` |
| 名前が `p` で始まる | `.items[] \| select(.name \| test("^p"))` | `name: pen` `price: 120` |
| 条件に合う商品の**名前だけ** | `.items[] \| select(.price > 500) \| .name` | `book` |

- 比較は `==`（等しい）`!=`（違う）`>` `>=` `<` `<=`。文字は **`"` で囲みます**（`"pen"`）。数字は囲みません
- 条件の組み合わせは `and`（かつ）`or`（または）`not`
- `test("正規表現")` は、文字が「パターンに合うか」を調べます（`"^p"` は「p で始まる」）

### 4-5b. 比べる型をそろえる

**比べる相手の型に注意**します。`.price > "a"`（数字と文字の比較）は、`評価エラー：!!int not yet supported for comparison` になります。数字は数字と、文字は文字と比べましょう。

### 4-6. 形を作り替える：`{ }` `[ ]` と `map`

取り出した値を、**新しい形にまとめ直す**ときに使います。

| やりたいこと | 式 | 結果 |
|---|---|---|
| 新しい項目にまとめる | `{"port": .server.port}` | `port: 8080 # 開発用` |
| 名前だけのリストにする | `.items \| map(.name)` | `- pen` `- book` |
| 項目名を付け替える | `.items \| map({"name": .name, "yen": .price})` | `- name: pen` `yen: 120` / `- name: book` `yen: 980` |
| 条件に合うものだけ残す（リストのまま） | `.items \| map(select(.price < 500))` | `- name: pen` `price: 120` |

- `{ 新しい名前: 式 }` は「新しい項目を作る」、`[ 式 ]` は「リストにまとめる」です
- `map( 式 )` は「**リストの全要素に同じ式を適用して、リストで返す**」ものです
- `select` を `.items[]` と使うと**1 件ずつ**、`map(select(...))` と使うと**リストのまま**返ります

### 4-7. 計算・並べ替え・最大最小

| 式 | 意味 | 結果 |
|---|---|---|
| `.server.port + 1` | 足し算（`-` `*` `/` `%` も可） | `8081` |
| `.items[].price * 2` | 全商品の価格を 2 倍 | `240` `1960` |
| `.items[] \| .name + "!"` | 文字はつなげられます | `pen!` `book!` |
| `.items \| map(.price) \| max` | 最大 | `980` |
| `.items \| map(.price) \| min` | 最小 | `120` |
| `.items \| map(.price) \| .[0] + .[1]` | 1 つ目と 2 つ目の合計 | `1100` |
| `.items \| sort_by(.price) \| .[0].name` | 価格の**安い順**に並べて、先頭の名前 | `pen` |

**合計を出したいとき**：jq には `add`（全部足す）がありますが、**yaqpy にはまだありません**。上の表のように `.[0] + .[1]` と書くか、`max` / `min` / `length` を使ってください（[4-10](#4-10-まだ使えないもの)）。

### 4-8. 「無いとき」の備え：`//` `?` `has`

| 式 | 意味 | 結果 |
|---|---|---|
| `.server.timeout // 30` | 値が**無い（null）なら 30** | `30` |
| `.server.port // 0` | 値があればそれ | `8080` |
| `has("server")` | `server` というキーが**あるか** | `true` |
| `.server \| has("tls")` | 入れ子の中にあるか | `true` |
| `.server.port?` | 取り出せなくてもエラーにしない | `8080` |

### 4-9. 値を書き換える

式で**値を書き換えた結果**を見ることもできます。右の窓に「書き換え後の全体」が出ます。

| 式 | 意味 | 結果（抜粋） |
|---|---|---|
| `.server.port = 9090` | 値を**置き換える** | `port: 9090 # 開発用`（コメントも残ります） |
| `.items[].price \|= . + 10` | 全商品の価格を **10 足す** | `price: 130` / `price: 990` |
| `.server.hosts += ["c"]` | リストに**追加** | `hosts: [a, b, c]` |
| `del(.server.hosts)` | 項目を**消す** | `hosts:` の行が無くなる |

> **ここで大事なこと**：書き換えたのは**右の窓の結果だけ**で、**開いたファイルは変わりません**。結果を残したいときは、右上の `[保存]` で**別の名前**で保存します（[8 章](#8-保存とコピー)）。
>
> `.items[] | select(.price > 500) | .name = "BOOK"` のように `select` で絞ってから書き換えると、**絞った要素だけ**が結果に出ます（全体は出ません）。全体の中で書き換えたいときは `|=` を使ってください。

### 4-10. まだ使えないもの

jq の解説記事によく出てくる機能のうち、**`add` と `load(...)` は、yaqpy では今のところ使えません**（yaqpy は yq という別のツールを手本にしていて、実装している演算子が少し違います）。`unique` `reverse` `join` `reduce` と、文字列の中に式を埋める `"\(.a)"` は、v0.3.0 から使えます。

| 記事にある機能 | yaqpy | 書き方 |
|---|---|---|
| `add`（全部足す） | ✕ 式の解析でエラー | `.[] as $x ireduce (0; . + $x)`、または `.[0] + .[1]` のように足す |
| `unique`（重複を消す） | ○（v0.3.0〜） | `unique`、キーで判定するなら `unique_by(.name)` |
| `reverse`（逆順） | ○（v0.3.0〜） | `reverse` |
| `join(",")`（文字をつなぐ） | ○（v0.3.0〜） | `join(",")` |
| `reduce`（畳み込み） | ○（v0.3.0〜） | `.[] as $x ireduce (0; . + $x)` |
| 文字列の中に式を埋める | ○（v0.3.0〜） | `"名前は \(.name)"` |
| `load(...)`（他のファイルを読む） | ✕ `unknown operator LOAD` | （なし） |

使える演算子の一覧は [USAGE.ja.md](./USAGE.ja.md#演算子) にあります。

### 4-11. コメントと型を調べる

YAML の**コメント**も式で取り出せます（GUI の左右の窓でコメントが残るのは、この仕組みのおかげです）。

| 式 | 意味 | 結果 |
|---|---|---|
| `.server.port \| line_comment` | 行末のコメント | `開発用` |
| `. \| head_comment` | 先頭のコメント | `サーバー設定` |
| `.server.port \| tag` | 値の型 | `!!int`（整数） |

### 4-12. よく使う式の早見表

| こんなとき | 式 |
|---|---|
| 特定の値だけ見たい | `.server.port` |
| リストの全部の名前 | `.items[].name` |
| 条件に合うものだけ | `.items[] \| select(.price > 500)` |
| その中の名前だけ | `.items[] \| select(.price > 500) \| .name` |
| 何件あるか | `.items \| length` |
| キーの一覧 | `.server \| keys` |
| 別の形にまとめる | `.items \| map({"name": .name, "yen": .price})` |
| 値が無いときの既定値 | `.server.timeout // 30` |
| キーがあるか | `has("server")` |
| 値を変えた結果を見る | `.server.port = 9090` |

### 4-13. 式が難しいとき：AI に相談する

下の **`[AIに相談]`** タブを開くと、AI（ChatGPT・Claude など）に式の書き方を相談するための文面を、**組み立てながら**作れます（コマンドの `yaqpy --guide-prompt` と同じ内容が土台。v0.5.0 は式バーの `[🤖]` のダイアログでしたが、v0.7.0 で専用のタブになりました）。

```text
┌ あなたの入力（データ例・やりたい変換）┐ ┌ AIへの相談文（コピーしてAIに貼り付け）┐
│ この JSON から price が 500 超の       │ │ # yaqpy の式を書いてください          │
│ name だけ取り出したい                  │ │ …（記法・演算子の一覧・注意書き・例）… │
│                                        │ │ ## 依頼                               │
│                                        │ │ （ここに、入力データの例と、…）       │
│ [貼り付け] [クリア] [＋プロンプトに反映]│ │ [クリア] [初期状態に戻す] [コピー]    │
└────────────────────────────────────────┘ └───────────────────────────────────────┘
```

- **左**に、データの例とやりたい変換を書き（`[貼り付け]` でクリップボードから入れられます）、**`[＋プロンプトに反映]`** を押すと、右の相談文の「## 依頼」の案内の一文が、左の内容に置き換わります。**押したときだけ**右に入ります（右を直接編集した内容を、勝手に上書きしないためです）
- **2 回目以降**（案内の一文がもう無いとき）は、右の相談文の**末尾**に、空行をはさんで追記します。やり直したいときは `[初期状態に戻す]` で、作りたての相談文に戻せます（編集や反映は捨てられます）
- 出てくる文面には、式の記法・**実装済み／未実装の演算子の一覧**・やってはいけない書き方・実行例が入っています（すべて実際に確かめたものです）
- 右の全文は自由に編集できます。`[コピー]` で全文をクリップボードに入れて、そのまま AI に貼り付けます
- 文書を開いていなくても使えます。相談文は、タブを**初めて開いたときに 1 回だけ**作ります
- クリップボードの読み書きは、ブラウザや OS が許可しないと失敗します。そのときは「貼り付けられませんでした」などと案内が出るので、欄をクリックして `Ctrl + V` などで入れてください

### 4-14. 原文を書き換える：追加編集

読み込んだあとも、左の窓（オリジナル）に**直接書き足したり書き換えたりできます**（v0.5.0。詳しくは [7-2](#7-2-原文を書き換える追加編集)）。

---
[toTop](#toreadme)
## 5. プロパティのプルダウンを使いこなす

式を覚えていなくても、**文書の中身から候補を作って選べる**のがプルダウン（③）です。

### 5-1. 使い方

1. ファイルを開くと、候補が自動で作られます（ふだんはこれだけです）
2. `プロパティ` 欄をクリックすると候補の一覧が出ます。**文字を打つと絞り込まれます**（`port` → `.server.port` だけ）
3. 候補を選ぶと、**式欄がその候補に置き換わり、すぐ実行されます**
4. `[+ パイプを追加]`（v0.7.0 で「式に追加」から変わりました）は、プルダウンとは**別に**、いまの式の**末尾に ` | ` を足すだけ**のボタンです。カーソルの位置は取れないので、いつも末尾です。式が空のあいだは押せません

たとえば式欄が `.items[]` のとき、`[+ パイプを追加]` を押すと `.items[] | ` になります。続きは自分で書くか、候補を選んで（式欄が置き換わります）書き直します。**すでに末尾がパイプのとき**は、重ねません。押しても**実行はされません**（続きを書いてから実行します）。

### 5-2. `sample.yaml` の候補（13 件）

| 候補 | 種類 | 値の例 |
|---|---|---|
| `.server` | 入れ物（map） | |
| `.server.port` | 値 | 8080 |
| `.server.hosts` | リスト（seq） | |
| `.server.hosts[]` | 値 | a |
| `.server.tls` | 入れ物 | |
| `.server.tls.enabled` | 値 | true |
| `.server.tls.cert` | 値 | /etc/cert.pem |
| `.backup` | 入れ物 | |
| `.backup.schedule` | 値 | 0 3 * * * |
| `.items` | リスト | |
| `.items[]` | 入れ物 | |
| `.items[].name` | 値 | pen |
| `.items[].price` | 値 | 120 |

プルダウンには `.server.port  = 8080` のように、**値の例**も並びます。値の例で絞り込むこともできます。

### 5-3. 候補の作られ方（知っておくと迷いません）

- **リストは `[]` にまとめます。** 要素ごとには展開しません（`.items[0].name` ではなく `.items[].name`）。中身の形は**先頭の要素**を見本にします
- **YAML の「アンカー／マージ」は辿りません。** `sample.yaml` の `backup` は `<<: *tls` で `tls` を取り込んでいますが、`.backup.enabled` は候補に出ません（式に手で書けば使えます）
- **深さは 6 段、候補は 500 件まで**です。多いときは「候補が多いため、絞り込んでください」と出ます
- **キー名に空白や記号があると、`.["キー名"]` の形で出ます**（例：`my key` → `.["my key"]`、`9num` → `.["9num"]`）。**候補はすべて、そのまま実行できる式**です
- 候補は**ファイルを開いた直後に 1 回**作ります。式を変えても作り直しません
- 候補が作れない文書（壊れた YAML、値だけの文書など）では、プルダウンが**灰色**になって「この文書からはプロパティ候補を作れませんでした」と出ます。**このときも式欄は使えます**

---
[toTop](#toreadme)
## 6. 入力形式・出力形式・見た目の設定

② の形式バーで、**読み方**と**書き方**を選べます。`auto` のままでも、実際にどの形式で読み・書くかは、左右の窓の見出しの横の札で分かります（入力が `auto` ならファイル名の拡張子で決まり、出力が `auto`（表示は **`auto（入力と同じ）`**）なら入力と同じ形式です）。**出力形式の既定は `yaml` です**（v0.7.0）。JSON や XML などを開いても、最初から YAML で表示されます。元の形式のまま見たいときは、出力形式を `auto（入力と同じ）` に選び直してください。

### 6-1. 入力形式（読み方）

- `auto`（既定）：**ファイルの拡張子**で判定します（`.json` → JSON、`.xml` → XML、`.csv` / `.tsv` → CSV / TSV、`.toml` → TOML、`.properties` → properties、`.toon` → TOON）。**拡張子で決まらないとき**（貼り付けたテキスト、見覚えのない拡張子・拡張子なしのファイル）は、**中身を見て判定**します（yaqpy 独自の拡張。見分けられないときは YAML）
- `yaml` / `json` / `xml` / `csv` / `tsv` / `toml` / `props` / `toon`：手で指定します。**自動判定と食い違っているとき**に使います。ここに出る形式は、コマンドで読める形式と同じです

たとえば中身が YAML なのに `data.json` という名前のファイルを開くと、`読み書きに失敗しました：bad JSON: …` と出ます。**入力形式を `yaml` に切り替える**と、そのまま読めて、プロパティ候補も戻ります。

### 6-2. 出力形式（書き方）

出力形式は**既定で `yaml`** です。`auto（入力と同じ）` を選ぶと、入力と同じ形式で出します（コマンドの `-o` を省いたときと同じ規則で、コマンド側の既定は変えていません）。`.items` を例にすると、こう変わります。

```yaml
# yaml
- name: pen
  price: 120
- name: book
  price: 980
```

```text
# props（Java の設定ファイル形式）
0.name = pen
0.price = 120
1.name = book
1.price = 980
```

```text
# toon（AI に渡すためのコンパクトな形式）
[2]{name,price}:
  pen,120
  book,980
```

`json` は `{ "name": "pen", "price": 120 }` のような JSON になります。XML・CSV / TSV・TOML・properties の読み書きの規則は [USAGE.ja.md](USAGE.ja.md) の各節にあります（TOML は読むときコメントが保持されません）。

### 6-3. インデント（字下げの幅）

- **インデント**：字下げの数です。`2` → `4` にすると、`server:` の下が 4 文字ぶん下がります。`0` にすると JSON が 1 行にまとまります。数字欄に直接打つほか、**両脇の `－`/`＋` ボタンでも 1 ずつ増減**できます（v0.5.0。`0` より下には行きません）
- **整形（-P）のスイッチは、v0.7.0 で画面から外しました。** `[a, b]` のような 1 行の書き方を 1 項目 1 行にしたいときは、式を `.. style=""`（全体）や `.server | (.. style="")`（`.server` の中だけ）のようにするか、コマンドの `-P` を使ってください（どちらも実際に確かめたものです）

形式とインデントは、**変えるたびに自動で再実行**されます（この自動の再実行は、[実行ログ](#16-実行ログとログ画面v070)には記録されません）。

---
[toTop](#toreadme)
## 7. ファイルの開き方（貼り付けも使えます）

- **`[＋ファイルを追加]`**：ダイアログで選びます（v0.5.0 から、以前の「ファイルを開く」と 1 つのボタンに統一：**最初から押せます**。何も開いていなければ選んだ最初の 1 件がそのまま開き、すでに開いていれば閉じずに増やします。複数選択も可）。**拡張子では絞りません**（どんなファイル名・拡張子でも選べます）。開いたら、拡張子が形式を決められるときは拡張子、決められないとき（見覚えのない拡張子・拡張子なし）は中身を見て判定します（次項）
- **貼り付け**：何も開いていない状態の左の窓に、テキストを**貼り付ける**と、そのまま 1 つの文書として開きます（**入力が止まって 0.3 秒後**に取り込みます）。ちょっとした確認や、メールの切り抜きを試すのに便利です
- **`[ファイルを開く]` と `[貼り付け]` のボタン**（v0.7.0）：何も開いていないときの画面にも、案内の下にこの 2 つのボタンが出ます（枠全体をクリックしても開けます）。`[貼り付け]` はクリップボードの内容を 1 つの文書として開きます（許可されないときは案内が出ます）
- **ドラッグ＆ドロップ**：**Web 版では対応しました**（v0.7.0。[15-2](#15-2-画面の違い)）。**デスクトップ版には、まだありません**（Flet の標準クライアントがファイルのドロップを受けられないためです。何も開いていないときの画面にも、その旨が出ています）
- **`[閉じる]`**：**開いているものをすべて**閉じて、最初の状態に戻します（貼り付け欄も空に戻ります）。1 件だけ閉じたいときは [7-1](#7-1-複数ファイルを開く) のチップの `×` を使います
- **開けるもの**：UTF-8 のテキストで、**50 MiB 以下**（[設定](#9-設定画面)で変えられます）。フォルダや、UTF-8 でないファイルは「開けません」と出ます
- **貼り付けた文書には名前がないので、中身を見て形式を判定します**（yaqpy 独自の拡張。JSON・TOML・properties・CSV/TSV・XML はそれと分かる書き方なら判定し、見分けられなければ YAML として読みます。JSON は YAML の一種としても読めるので、結果は変わりません）

練習に使う小さな例です。何も開いていない状態で、次を貼り付けてみてください。

```yaml
name: pasted
items:
  - id: 1
  - id: 2
```

`.items[].id` を実行すると `1` と `2` が出ます。

### 7-1. 複数ファイルを開く

**v0.5.0 から、2 つ以上のファイルを同時に開いておけます。**

- ファイルを 1 つ開いたあと、ファイル名の右にある **`[＋ファイルを追加]`** で、もう 1 つ（複数選択も可）を**閉じずに**追加できます
- 追加すると、ファイル名の下に**チップ**（名前の付いた札）が並びます。チップをクリックすると、その文書に切り替わり、**いま入っている式がそのまま**その文書に対して実行されます（同じ式を複数のファイルに当てて見比べる、という使い方を想定しています）
- チップの `×` で、その 1 件だけを閉じられます。最後の 1 件を閉じると、最初の画面に戻ります
- **チップは先頭の 2 件だけ**が並び、3 件目以降は **`[＋ファイル N件]`** の 1 つのボタンにまとまります（v0.7.0）。押すと一覧が開き、選ぶとその文書に切り替わります。`閉じる: 名前` の項目で、その 1 件を閉じられます。並び順は**開いた順のまま**で、3 件目以降の文書を選んでも、チップ側には繰り上がりません（そのときは `[＋ファイル N件]` が選択中の色になり、一覧にチェックが付きます）
- 保存・コピーの対象は、**いまチップで選んでいる文書の結果**です

> **「まとめて評価」について**：複数の文書を CLI の `eval-all`（`select(fi == 0) * select(fi == 1)` のように 1 回の評価にまとめる）と同じ形で扱う機能は、v0.5.0 の開発中に画面へのトグルとして一度試しましたが、**入力形式が異なる文書を混ぜると変換に失敗する**ことがあり、「どんな場面で使うか」「結果を 1 通にまとめるのか複数のまま返すのか」をもう少し詰めてからにする方針にし、**画面からは撤去しました**。コマンドの `yaqpy eval-all` 自体は今までどおり使えます（[13 章](#13-コマンドcliとの対応)）。

### 7-2. 原文を書き換える（追加編集）

**v0.5.0 から、読み込んだあとの左の窓（オリジナル）に直接書き足したり書き換えたりできます。**

- 打ち終わって **0.5 秒止まると**、書き換えた内容を取り込んで**自動で再実行**します（貼り付けと同じ考え方です）
- 書き換えると、見出しの横に赤字で **「追加編集」** と出ます。文書ごとに覚えているので、[7-1](#7-1-複数ファイルを開く) のチップを切り替えても、それぞれの編集内容と「追加編集」の表示は保たれます
- **開いたファイルそのもの（ディスク上の内容）は変わりません。** ここで書き換わるのは、画面の上の入力（`original_text`）だけです。上書き保存すると、その時点の**ディスク上の元の内容**を `.bak` にバックアップしてから書き込みます（[8-2](#8-2-元のファイルを守る仕組み)）。「追加編集」は保存の可否には関係しません
- 別のファイルを新しく開く（`[＋ファイルを追加]` で 0 件の状態から開く、または `[閉じる]` のあと）と、「追加編集」の表示は消えます

```yaml
# 例：sample.yaml を開いたあと、左の窓の port を 9090 に書き換えて 0.5 秒待つ
server:
  port: 9090 # 開発用
```

式が `.server.port` なら、右の窓は自動で `9090` に変わります。

---
[toTop](#toreadme)
## 8. 保存とコピー

### 8-1. 保存

右上の `[保存]` で、**右の窓の結果**を別名のファイルに保存します。

- **ファイル名の初期値**は、開いたファイルの名前 ＋ 出力形式の拡張子です（`sample.yaml` を json で出力 → `sample.json`、props → `sample.properties`）。貼り付けた文書は `output.yaml` などになります
- 保存すると、下に「保存しました：…」と出ます
- 保存内容は、**画面の見た目ではなく結果の全部**です。大きな結果は画面には**先頭 5000 行**までしか出ませんが（[設定](#9-設定画面)で変えられます）、保存は**全量**です
- 式にエラーがある間や、実行中は `[保存]` を押せません（古い結果が保存されるのを防ぐためです）

### 8-2. 元のファイルを守る仕組み

- **開いているファイルと同じ場所・同じ名前**で保存しようとすると、「**元のファイルを上書きしますか？**」と確認が出ます。**既定のボタンは「やめる」**です（Enter で元のファイルは無傷）
- すでにあるファイルを選ぶと、Windows などの**OS の上書き確認**も出ます
- **TOML のファイルを上書きすると、コメントが消えます**（読むときにコメントを保持しないため。コマンドの `-i` は既定で拒否しますが、GUI の上書きは確認のうえ実行できます）。コメントを残したいときは、別名で保存してください
- 保存は「一時ファイルに書いてから置き換える」方式なので、途中で失敗しても保存先は壊れません

### 8-3. コピー

`[📋]` は、結果の**全量**をクリップボードにコピーします（「変換結果をクリップボードにコピーしました」）。

### 8-4. 式をファイルに保存・読み込み（`.yaqpy`）

式欄の右にある 2 つのアイコン（v0.7.0）で、**式だけ**をファイルに保存し、あとで読み込めます。

- **`.yaqpy` ファイルの中身は、式のテキストそのもの**です（UTF-8・改行は LF・末尾に改行 1 つ）。複数行の式もそのまま入ります。コマンドの `yaqpy --from-file sample.yaqpy data.json`（`yaqpy sample.yaqpy data.json` でも可）でそのまま使えます（[USAGE.ja.md](USAGE.ja.md)）。同梱のレシピ（`--recipe`）の式ファイルも同じ拡張子です
- **`[式を保存]`**：ファイル名の初期値は、開いている文書の名前の拡張子を除いたもの ＋ `.yaqpy`（`sample.yaml` → `sample.yaqpy`。貼り付けや文書なしなら `expression.yaqpy`）。式が空のあいだは押せません。既存のファイルを選んだときの上書き確認は、OS のダイアログに任せます。**Web 版**は、ブラウザのダウンロードになります
- **`[式を読み込む]`**：`.yaqpy` と `.yq`（mikefarah/yq の式ファイル）を選べます（**Web 版**はアップロード）。読み込むと式欄が**置き換わります**（確認はありません。あとで保存できるためです）。**式に誤りがあっても読み込み**、式欄の赤枠でお知らせします。**実行はしません**（実行ボタンを押します）。文書を開く前でも使えて、読み込んだ式は、その後で文書を開いても**残ります**（全部閉じたときだけ `.` に戻ります）
- 上限は 1 MiB です（誤って巨大なファイルを選んだときに固まらないため）

---
[toTop](#toreadme)
## 9. 設定画面

下の `[設定]` で開きます。

| 項目 | 既定 | 意味 | 次に起動したとき |
|---|---|---|---|
| env / strenv を許可 | **不許可** | `strenv(HOME)` のように、**環境変数を読む式**を許可します | **必ず不許可に戻ります**（安全のため、許可は持ち越しません） |
| load / loadstr を許可 | **不許可** | 他のファイルを読む式のための許可です。**ただし、`load` は今のところ未実装**なので、許可しても使えません | **必ず不許可に戻ります** |
| タイムアウト（秒） | 10 | 1 回の実行にかけてよい時間です | **前回の値を覚えています** |
| 最大入力（MiB） | 50 | 開けるファイルの上限です | **前回の値を覚えています** |
| 表示行数の上限 | 5000 | 右の窓に出す行数の上限です（保存・コピーは全量） | **前回の値を覚えています** |
| ダークテーマ | オフ | 暗い配色にします | **前回の値を覚えています** |
| 表示言語 | 日本語 | 画面の文言（ボタン・ラベル・案内・エラーの見出し）を日本語／英語で切り替えます（v0.5.0） | **前回の値を覚えています**。**次回の起動から**効きます（すでに開いている画面の文字は変わりません） |
| 実行ログを記録する（v0.7.0） | オン | `[実行]` で成功したときだけ記録します（[16 章](#16-実行ログとログ画面v070)。**デスクトップ版のみ**） | **前回の値を覚えています** |
| 実行ログの保存先 | 空（既定） | 空なら OS ごとの既定の場所。フォルダを直接入力するか `[参照…]` で選びます。`[既定に戻す]` で空に戻せます。変えても、これまでのログは**移動しません** | **前回の値を覚えています** |
| 保存する件数の上限 | 500 | 超えたら古いものから削除します | **前回の値を覚えています** |
| 1件あたりの本文の上限（MiB） | 1 | 入力（合計）または結果がこれを超えたら、その本文は記録しません | **前回の値を覚えています** |

- **v0.5.0 から、タイムアウト・最大入力・表示行数の上限・ダークテーマは次の起動でも引き継がれます。** env / load の許可だけは、危険な許可を持ち越さないため毎回既定（不許可）に戻ります
- **設定を変えると、次の実行から効きます。** 「env を許可」と「表示行数」は、変えた瞬間に自動で再実行します
- **`system` 演算子（外部コマンドの実行）は、GUI では提供しません**
- 「env を許可」は環境変数を、「load を許可」は他のファイルを読む式のためのスイッチです。**load の方が読める範囲が広いため、別々に許可できる**ようにしています。**load / loadstr は今のところ未実装**なので、許可しても動作は変わりません（v0.7.0 で画面にも書きました）
- 数値欄を空にしたり `0` にしたりしても、設定は壊れません。欄を離れると、実際に使われている値が表示し直されます
- コマンド（CLI）は既定で env を**許可**しますが、GUI は安全のため既定で**不許可**です
- **英語表示の対象は画面の文言（ボタン・ラベル・案内・エラーの見出し）だけです。** 式の構文エラーの「N 文字目でエラー」や YAML エラーの「N 行 M 列」のような、**エラーの詳しい内容は今のところ日本語のまま**です（`gui/errors_ja.py`。将来の課題）

### 許可されていない式を書いたとき

`strenv(HOME)` を実行すると、状態バーに次のように出ます。

> この式は 環境変数（env / strenv） を使いますが、許可されていません（設定画面で「環境変数（env / strenv） を許可」を有効にすると実行できます）

横の `[設定を開く]` を押すと設定画面に移り、**該当のスイッチが色つきで強調**されます。ON にしてメインに戻ると、**自動で再実行**されて結果が出ます。

---
[toTop](#toreadme)
## 10. エラーの読み方

エラーは 2 か所に出ます。**式欄の赤枠**（打っている最中）と、**状態バーの赤い文字**（実行したとき）です。

### 10-1. 式の書き間違い（実行前に分かる）

式欄に打ち終えて **0.3 秒後**に検査され、間違いは**赤枠と `^` の位置**で示されます。**間違ったままでは実行されません**（結果の窓は空になります）。

```text
式の 8 文字目でエラー：bad expression - probably missing close bracket on SELF
.server.(
       ^
```

| 表示 | 原因 | 直し方 |
|---|---|---|
| `…probably missing close bracket on SELF` | `(` を閉じていない | 括弧の数をそろえる |
| `…probably missing close bracket on SELECT` | `select(` の `)` が無い | `select(条件)` と閉じる |
| `bad expression, could not find matching ]` | `[` を閉じていない | `]` を足す |
| `bad expression, got close brackets without matching opening bracket` | 閉じ括弧が余っている | 余分な `)` `]` を消す |
| `unexpected character 'x' at position N` | N 文字目（0 始まり）に、**解釈できない文字**。よくあるのは、**未実装の関数名**（`add` など）や、`..` の打ち間違い | その位置の文字を見直す。関数が使えるかは [4-10](#4-10-まだ使えないもの) |

`^` の位置は「N 文字目」と表示の数え方（1 始まり）が 1 ずれます（`position 7` → `8 文字目`）。

### 10-2. 実行してから分かるエラー（状態バー）

| 表示 | 原因 | 直し方 |
|---|---|---|
| `評価エラー：unknown operator XXX` | 知らない関数名を使った | 綴りを直す／[使えない機能](#4-10-まだ使えないもの)の代替に |
| `評価エラー：!!int not yet supported for comparison` | 数字と文字を比べた | 型をそろえる（数字は `"` で囲まない） |
| `この式は 環境変数（env / strenv） を使いますが、許可されていません` | env が不許可 | [設定](#9-設定画面)で許可 |
| `時間切れで中断しました（設定画面でタイムアウトを延ばせます）` | 実行が長すぎた | 式を軽くする／タイムアウトを延ばす |
| `中止しました` | `[■ 中止]` を押した | （正常です） |
| `YAML の 2 行 4 列 でエラー：…` | ファイルの YAML が壊れている | 行と列を見て直す。**左の窓には原文が残ります** |
| `読み書きに失敗しました：…（入力形式の選択が違うかもしれません）` | 入力形式が合っていない | [入力形式](#6-入力形式出力形式見た目の設定)を選び直す |
| `ファイルが大きすぎます（… / 上限 …）` | 50 MiB（既定）を超えている | [設定](#9-設定画面)で上限を上げる |
| `UTF-8 のテキストとして読めませんでした` | 文字コードが違う／テキストではない | UTF-8 で保存し直す |

**どのエラーでも、アプリは落ちません。** 原文は左の窓に残り、式を直してやり直せます。

---
[toTop](#toreadme)
## 11. 大きなファイルと「中止」

- **実行中は画面が固まりません。** 進捗バーが動き、`[■ 中止]` が押せます。窓の大きさも変えられます
- 実行中は、`[▶ 実行]` `[保存]` `[📋]` が押せません
- 式を計算している間の `[■ 中止]` は、**すぐ効きます**（重い式でも 1 秒以内に止まります）
- **ファイルの読み込み（デコード）中も、v0.5.0 から途中で止まるようになりました**（YAML・JSON・XML・CSV/TSV・TOML・properties・TOON のすべて）
- ただし**結果の書き出し（エンコード）の最中**は、その処理が終わるまで止まりません。押すと「中止を要求しました。書き出しの途中は、その処理が終わってから止まります」と出て、終わり次第止まります
- 実行中に形式などを変えると、いまの実行が終わってから**もう 1 回だけ**実行されます（古い設定の結果が残らないようにするためです）

---
[toTop](#toreadme)
## 12. 困ったとき（Q&A）

**Q. `uv tool install` / `uvx` で `yaqpy-gui` を起動すると、`Installing flet-desktop ... OK` と出たのに `ModuleNotFoundError: No module named 'flet_desktop'` で終了します**
A. `flet-desktop`（窓を描画する部品）の自動インストール先が、`yaqpy` 用の隔離された実行環境とは違う場所（起動したときのカレントディレクトリ付近にたまたまある、無関係な `.venv` など）になってしまったことが原因です。「OK」と出るのはそちらへのインストールが成功しているためで、`yaqpy` 自身の実行環境には入っていません。**`uv tool install --force "yaqpy[gui]" --with flet-desktop`** で入れ直してください（[1 章](#1-起動する)）。`pip install` の場合は `pip install "yaqpy[gui]" "flet-desktop==<入っている flet と同じ版>"` のように明示的に入れると同じ問題を避けられます。

**Q. 式を打っても何も起きません**
A. 打っただけでは実行されません。**Enter**（または `[▶ 実行]`）を押してください。ただし、プルダウンで候補を選んだとき、形式・インデントを変えたときは、自動で実行されます。

**Q. 結果が `null` になります**
A. たどったキーが存在しません。綴りや階層を確認するか、プルダウンから選び直してください（[4-2](#4-2-キーをたどる)）。

**Q. 結果が空です（何も出ません）**
A. `select` の条件に合う要素が 1 つも無いと、結果は空になります。条件を見直してください。

**Q. コメントが消えました**
A. YAML → YAML ではコメントが残ります。**JSON や TOON に出力するとコメントは消えます**（それらの形式にコメントが無いためです）。

**Q. 書き換えたのにファイルが変わっていません**
A. 仕様です。`[保存]` の**既定は別名保存**で、開いたファイルは書き換えません（[4-9](#4-9-値を書き換える)）。同じ場所・同じ名前を選んで「上書きする」と答えれば上書きできます（v0.5.0。[8-2](#8-2-元のファイルを守る仕組み)）。

**Q. `[保存]` が押せません**
A. 式にエラーがあるか、実行中です。エラーを直すか、実行が終わるのを待ってください。

**Q. 窓を閉じたのにコマンド画面が戻りません**
A. 通常は窓を閉じるとすぐ終了します。戻らない場合はコマンド画面で `Ctrl + C` を押してください。

**Q. 別のファイルを開くと、式はどうなりますか**
A. `[＋ファイルを追加]` は、**何も開いていないとき**は今までの文書をすべて閉じて式を `.`（全体）に戻し、**すでに開いているとき**は今の文書と式をそのまま残して追加します（v0.5.0。[7-1](#7-1-複数ファイルを開く)）。一度すべて閉じてから開き直したいときは `[閉じる]` を先に押してください。

---
[toTop](#toreadme)
## 13. コマンド（CLI）との対応

GUI でやることは、コマンドでもできます（書き方はほぼ同じで、`file.yaml` は開くファイル名です）。

| GUI の操作 | 相当する CLI |
|---|---|
| アプリを起動する | `yaqpy --gui`（または `yaqpy-gui`） |
| ファイルを開く | `yaqpy '.' file.yaml` |
| 式に `.server.port` を入れて実行 | `yaqpy '.server.port' file.yaml` |
| 出力形式を json にする | `yaqpy -o json '.' file.yaml` |
| インデントを 4 にする | `yaqpy -I 4 '.' file.yaml` |
| 整形（-P）の代わりに式 `.. style=""`（v0.7.0 で整形スイッチは画面から外しました） | `yaqpy -P '.' file.yaml` |
| 結果を別名保存 | `yaqpy '.' file.yaml > out.yaml` |
| 上書き保存（v0.5.0） | `yaqpy -i '式' file.yaml`（GUI は上書き前に `file.yaml.bak` を自動作成） |
| 起動時にファイルを指定（v0.5.0） | `yaqpy --gui file.yaml` |
| 複数ファイルを開いて 1 件ずつ試す（v0.5.0） | GUI 独自の機能（[7-1](#7-1-複数ファイルを開く)）。複数の文書を**まとめて** 1 回で評価したいときは `yaqpy eval-all '式' file1.yaml file2.yaml` を直接使ってください（GUI にはまだありません） |
| AI への相談文を出す（v0.5.0。v0.7.0 から「AIに相談」タブ） | `yaqpy --guide-prompt` |
| 式をファイルに保存・読み込む（v0.7.0） | `.yaqpy` に保存したものは `yaqpy --from-file sample.yaqpy file.yaml`（`yaqpy sample.yaqpy file.yaml` でも可） |
| 実行ログ（v0.7.0） | GUI 独自の機能（[16 章](#16-実行ログとログ画面v070)） |
| 設定で env を許可 | CLI は既定で許可（`--security-disable-env-ops` で不許可）。GUI は既定で**不許可**（Web 版は常に不許可） |
| ブラウザで使う（v0.6.0） | `yaqpy --web`（または `yaqpy-web`。[15 章](#15-web-版ブラウザで使う)） |

式の書き方は GUI もコマンドも**同じ**です。GUI で式を試してから、コマンドやスクリプトに使う、という流れもおすすめです。

---
[toTop](#toreadme)
## 14. 参考資料と、この文書の確かめ方

**参考にした資料**

- [jq コマンドの解説記事（Qiita）](https://qiita.com/takeshinoda@github/items/2dec7a72930ec1f658af)：`.` `.キー` `.[]`、パイプ、`map`、集計など、**式の考え方**の説明に役立ちます。**記事の題名は yq ですが、中身は jq（JSON 用）の解説**です。yaqpy は yq という別のツールを手本にしているため、記事のうち `add` `unique` `reduce` `from_entries` などは書き方が違う、または未実装の場合があります。この文書の [4-10](#4-10-まだ使えないもの) を先に確認してください
- [USAGE.ja.md](./USAGE.ja.md)：コマンドの使い方と、実装済みの演算子の一覧
- [yq（Go 版）のドキュメント](https://mikefarah.gitbook.io/yq/)：式の言語の本家の説明です

**この文書の確かめ方**

この文書の式と結果は、`examples/sample.yaml` に対して**実際に実行して確認**したものです。GUI の動きは、次の自動テストと同じ内容を実機（デスクトップ版）でも確かめています。

| この文書の章 | 確かめているテスト |
|---|---|
| 3・4 章（式と結果） | `tests/unit/test_gui_presenter.py`（プロパティ・select・JSON 出力・空式など） |
| 5 章（プルダウン） | `tests/unit/test_gui_paths.py`（候補 13 件・リストの `[]` 化・アンカー・キーの書き分け）、`test_gui_presenter.py` の `CandidateTests` |
| 6 章（形式） | `test_gui_presenter.py`（拡張子での判定、形式を直すと候補が戻る）、`tests/unit/test_gui_state.py`（インデントなど） |
| 6 章（自動判定） | `tests/unit/test_gui_formats.py` の `ContentDetectionTests`（貼り付け・見覚えのない拡張子・拡張子なしの判定、既知の拡張子が中身より優先されること） |
| 6-3 章（インデントの ± ボタン） | `test_gui_main_page.py` の `IndentStepperTests` |
| 7 章（開く） | `tests/unit/test_gui_intake.py`（大きすぎる・フォルダ・UTF-8）、`tests/unit/test_gui_main_page.py` の `OpenDialogTests`（拡張子で絞らないこと）・`UnifiedAddFileButtonTests`（開く／追加の統一）、`StartupFileTests`（`yaqpy --gui file.yaml`） |
| 7-1 章（複数ファイル） | `test_gui_presenter.py` の `MultiDocumentTests`、`test_gui_main_page.py` の `MultiFileUiTests`（画面には出ていない「まとめて評価」の内部ロジックは `EvalAllTests` に残っている） |
| 7-2 章（追加編集） | `test_gui_presenter.py` の `EditActiveDocumentTests`、`test_gui_main_page.py` の `EditableOriginalTests` |
| 4-13 章（AI に相談） | `test_gui_presenter.py` の `GuidePromptTests`、`tests/unit/test_gui_ask_ai.py`（プロンプトへの反映・初期状態に戻す・貼り付けの失敗） |
| 5-1 章（+ パイプを追加） | `test_gui_presenter.py` の `append_pipe` の各テスト、`test_gui_main_page.py` の `AddPipeTests` |
| 6 章（出力の既定は YAML） | `test_gui_main_page.py` の `OutputFormatDefaultTests` |
| 7 章（ボタン・チップのまとめ） | `tests/unit/test_gui_main_page_tools.py`、`test_gui_main_page.py` の `FileChipFoldingTests` |
| 8-4 章（`.yaqpy`） | `tests/unit/test_gui_expression_file.py`、`tests/unit/test_cli_args.py` の `ExpressionFileTests`、`tests/acceptance/test_cli.py` |
| 16 章（実行ログ） | `tests/unit/test_gui_run_log.py`（形式・読み戻し・上限・保存先の整理）、`test_gui_run_log_recording.py`（記録の条件）、`test_gui_log_presenter.py`・`test_gui_log_page.py`（ログ画面・再実行）、`test_gui_log_settings.py`・`test_gui_settings_log_section.py`（設定）、`test_yaml_emitter_blockscalar.py`（YAML の書き出し） |
| 8 章（保存） | `test_gui_presenter.py` の `SaveTests`（全量保存・上書き確認・上書き前のバックアップ・古い結果を保存しない）、`DefaultSaveNameTests` |
| 9 章（設定） | `test_gui_state.py`（既定は不許可・system は常に不許可）、`test_gui_presenter.py` の `SecurityTests`、`test_gui_prefs.py`（設定の保存・復元）、`test_gui_texts.py`・`test_gui_settings_page.py`（表示言語の切り替え） |
| 10 章（エラー） | `tests/unit/test_gui_errors_ja.py` |
| 11 章（中止） | `test_gui_presenter.py` の `CancelTests`（実行中の評価を別タスクから止める）、`tests/unit/test_format_decode_budget.py`（デコード中の中止） |
| 15 章（Web 版） | `tests/unit/test_gui_web_server.py`（実際にサーバーを起動：`127.0.0.1` だけで待ち受ける・画面が配られる・署名の無いアップロードを断る・終了時に一時フォルダを消す）、`test_gui_upload.py`（アップロードの順序・上限・書きかけを残さない）、`test_gui_cli_entry.py`（`yaqpy-gui` と `yaqpy-web` の引数・ヘルプ）、`test_cli_web_flag.py`（`yaqpy --web`）、`test_gui_logo.py`（ロゴ）、`test_gui_web_config.py`（上限・公開の判定・同時実行の関門）、`test_gui_presenter.py` の `WebSessionTests`（env / load を強制的に無効・ダウンロードは全量）、`test_gui_main_page.py` の `WebMainPageTests`・`WebSettingsPageTests` |

---
[toTop](#toreadme)
## 15. Web 版（ブラウザで使う）

v0.6.0 から、**同じ画面をブラウザで**使えます。アプリの窓の代わりに、自分の PC で小さな Web サーバーを動かし、ブラウザで開く形です。画面の見た目と操作（式・プルダウン・形式・複数ファイル・AI への相談文）はデスクトップ版と同じで、**ファイルの受け渡しだけが変わります**（15-3）。

### 15-1. 起動する

Web 版には、GUI の部品（flet）に加えて **flet-web** が要ります。`yaqpy[gui]` の代わりに **`yaqpy[web]`**（両方なら `yaqpy[gui,web]`）で入れます（`pip install "yaqpy[web]"` または `uv tool install "yaqpy[web]"`）。リポジトリを clone した場合は次のとおりです。

```bash
uv sync --extra web                  # Web 版の部品（flet と flet-web）が入ります
uv run yaqpy-web                     # http://127.0.0.1:8550/ で起動し、既定のブラウザが開きます
uv run yaqpy --web                   # こちらでも同じ（yaqpy-gui と yaqpy --gui の関係と同じです）
uv run yaqpy-web --port 9000         # ポート番号を変える（yaqpy --web --port 9000 でも同じ）
```

- 止めるときは、起動した端末（コマンド画面）で **`Ctrl + C`** を押します。ブラウザのタブを閉じてもサーバーは止まりません
- flet-web を入れずに起動すると、入れ方を案内して終了します
- オプションの一覧は `yaqpy-web --help`（デスクトップ版は `yaqpy-gui --help`）で見られます。`yaqpy --web` の場合は、`yaqpy` 自身のオプションではなく、下の表のオプションだけを書けます（`yaqpy --web --help` は `yaqpy-web --help` と同じ内容です）
- 起動時に、Web クライアントの描画部品と**日本語のフォントを CDN（インターネット）から**読み込みます（Flet の既定）。インターネットに出られない環境では `--no-cdn` を付けますが、**その場合は日本語の文字が「□」になります**（英数字は表示されます）。`--lang en` と一緒に使ってください

| オプション | 既定 | 意味 |
|---|---|---|
| `--host アドレス` | `127.0.0.1` | 待ち受けるアドレス。**既定はこの PC からだけ**。`0.0.0.0` などにすると警告が出ます（15-5） |
| `--port 番号` | `8550` | ポート番号 |
| `--lang ja\|en` | `ja` | 表示言語（**すべてのブラウザで共通**。設定画面では変えられません） |
| `--no-browser` | — | 起動時にブラウザを開かない |
| `--no-cdn` | — | CDN を使わない（日本語が表示できなくなります。上を参照） |
| `--max-input-mib N` | `10` | 受け付けるファイル・貼り付けの大きさの上限（MiB） |
| `--timeout 秒` | `10` | 1 回の実行の時間の上限 |
| `--max-concurrent-runs N` | `2` | すべてのブラウザを合わせて、同時に走らせる実行の数 |

Web 版にはファイル名を渡せません（15-3）。ファイルを開いた状態で起動したいときは、デスクトップ版の `yaqpy-gui ファイル`（`yaqpy --gui ファイル` と同じ）を使ってください。

ブラウザのタブのアイコン（と、読み込み中の画面）には yaqpy のロゴが出ます（デスクトップ版の窓のアイコンも同じロゴです。窓のアイコンは Windows でだけ変わります）。

### 15-2. 画面の違い

| 場所 | デスクトップ版 | Web 版 |
|---|---|---|
| `[＋ファイルを追加]` | ファイルを開く（パスで読む） | ブラウザから**アップロード**する。上限（既定 10 MiB）を超えるファイルは**送る前に**断られます |
| ファイルの**ドラッグ＆ドロップ**（v0.7.0） | 対応していません | **ページのどこにでもドロップできます**。`[＋ファイルを追加]` と同じ経路（サイズの事前確認・アップロード）を通ります。実機では、ブラウザ内の合成したドロップで確かめました（ファイルマネージャーからの実際のドラッグは未確認） |
| 式のファイル（`.yaqpy`） | ファイルとして保存・読み込み | 保存は**ダウンロード**、読み込みは**アップロード**（[8-4](#8-4-式をファイルに保存読み込みyaqpy)） |
| `[ログ]` タブ・実行ログ | あり（[16 章](#16-実行ログとログ画面v070)） | **なし**（サーバーに利用者のデータを残さないため） |
| 右上の保存ボタン | `[保存]`（別名保存・確認つきの上書き） | **`[ダウンロード]`**。変換結果の**全量**をブラウザのダウンロードで受け取ります。**上書き保存はありません**（サーバーには書きません） |
| コピー | OS のクリップボード | ブラウザの許可しだい。許可されないと「コピーできませんでした」と案内が出るので、欄の文字を選択してコピーしてください |
| `[⏻]`（終了） | あり | **なし**（タブを閉じる／サーバーは端末で `Ctrl + C`） |
| 設定画面 | env・load の許可、表示言語 | **env・load の許可のスイッチは出ません**（常に無効）。表示言語は `--lang` で決まります。上限（最大入力・タイムアウト）はサーバーの値が優先され、画面にも表示されます |
| 設定の保存先 | この PC | **ブラウザ**（同じブラウザなら次回も引き継がれます） |

### 15-3. ファイルの受け渡しと、サーバーに残るもの

- 開いたファイルは、ブラウザから**サーバーの一時フォルダへアップロードされ、読み込んだ直後に消されます**（中身はサーバーのメモリにだけ残ります）。タブごとの空のフォルダは、そのタブのセッションが破棄されるとき（Flet の既定では、タブを閉じてから約 1 時間後）に消え、サーバーを止めると一時フォルダごと消えます
- サーバー側のファイルを名前で開くことはできません（`yaqpy-web ファイル` がエラーになるのはこのためです）
- 同じブラウザで**タブを 2 つ**開くと、それぞれ別の画面（別のセッション）になり、開いたファイルや式は混ざりません。**同じタブの再読み込み**では、前の状態に戻ります

### 15-4. 安全のための決まり（変えられません）

| 決まり | 理由 |
|---|---|
| 既定では**この PC からしか開けない**（`127.0.0.1`） | 同じネットワークの誰かに開かれないため |
| **env / strenv・load / loadstr・system の演算子は常に無効**。設定画面にも許可のスイッチを出さない。さらに、Web 版の評価は**サーバーのファイル・環境変数に届かない仕組み**の上で動く | ブラウザを開いた人に、サーバー側の秘密（環境変数・ファイル）を読ませないため |
| 入力の大きさ・実行時間・同時実行の数に上限がある | 大きすぎる入力や重い式で、サーバーが止まらないため。同時実行が上限に達すると「サーバーが混み合っています」と表示されます |
| 式や文書の中身をログに残さない（アクセスログも出さない） | 端末の画面やログファイルに内容が残らないため |

### 15-5. ほかの PC から使いたいとき

`--host 0.0.0.0` のように指定すると、ほかの PC からも接続できるようになり、起動時に次のような**警告**が出ます。

```text
警告: --host 0.0.0.0 は、この PC 以外からも接続できる待ち受けです。
  yaqpy の Web 版には認証がありません。同じネットワークの誰でも画面を開けます。
  公開するなら、認証つきのリバースプロキシの後ろに置いてください。
```

- **yaqpy の Web 版には認証（ログイン）がありません**。公開する場合は、認証をかけたリバースプロキシ（nginx など）の後ろに置く前提です
- `http://` のまま別の PC から開くと、ブラウザの決まりにより**クリップボードへのコピーが使えない**ことがあります（コピーは「安全な接続」（`https://` か `localhost`）でしか許されないのが一般的です）。その場合は欄の文字を選択してコピーしてください

### 15-6. 困ったとき（Q&A）

**Q. 日本語が「□」になります**
A. `--no-cdn` を付けて起動していないか確認してください。付けていない場合は、CDN（インターネット）に接続できているか確認してください。インターネットに出られない環境では、`--lang en` を付けて英語の画面で使ってください。

**Q. 「ファイルが大きすぎます」と出ます**
A. 上限（既定 10 MiB）を超えています。サーバーを `--max-input-mib 20` のように上限を上げて起動し直すか、必要な部分だけのファイルにしてください。

**Q. 「サーバーが混み合っています」と出ます**
A. ほかのタブ（ほかの人）の実行が上限（既定 2 件）に達しています。少し待ってからもう一度 `[▶ 実行]` を押してください。

**Q. 「Web 版を起動できませんでした（127.0.0.1:8550）」と出て終了します**
A. 多くの場合、同じポートを別のプログラム（起動したままの yaqpy など）が使っています。直前の行に uvicorn の理由（Windows なら `[winerror 10048] 通常、各ソケット アドレスに対して…`）が出ます。`--port 8551` のように別の番号を指定してください。

**Q. 設定画面に env / load の許可がありません**
A. 仕様です（15-4）。環境変数やファイルを読む式を使いたいときは、デスクトップ版（`yaqpy-gui`）かコマンド（`yaqpy`）を使ってください。

---
[toTop](#toreadme)
## 16. 実行ログとログ画面（v0.7.0）

**デスクトップ版だけの機能**です（Web 版は、サーバーに利用者のデータを残さないため、記録もログ画面もありません）。

### 16-1. 何が記録されるか

- **`[実行]` ボタン（式欄の `Enter` を含む）で変換に成功したときだけ**、1 回につき **1 ファイル**を記録します
- 記録しないもの：形式・インデント・設定の変更やプロパティの選択による**自動の再実行**、**失敗した**実行、**直前に記録した内容と同じ**実行（式・入力・形式・インデント・許可の状態が同じ。アプリを起動し直すまで覚えています）、[ログ画面からの再実行](#再実行)
- [設定](#9-設定画面)の「実行ログを記録する」をオフにすると、記録しません（ログ画面での閲覧・再実行は、オフでも使えます）
- **ログには入力データと変換結果が平文で保存されます。** 機密情報を扱うときは記録をオフにしてください。`env` を許可していると、環境変数の値が結果に含まれることがあります（マスキングはできません）。入力ファイルのフルパスも記録に残ります

### 16-2. 保存先・ファイル名・上限

| OS | 既定の保存先 |
|---|---|
| Windows | `%LOCALAPPDATA%\yaqpy\logs` |
| macOS | `~/Library/Logs/yaqpy`（**実機未確認**） |
| Linux など | `$XDG_STATE_HOME/yaqpy/logs`（未設定なら `~/.local/state/yaqpy/logs`。**実機未確認**） |

- 保存先は[設定](#9-設定画面)で変えられます（フォルダを直接入力するか `[参照…]`。`[参照…]` が使えない環境では、直接入力してください）。起動したフォルダには作りません
- ファイルは `<保存先>/<年>/<月日>-<時分秒>_<入力形式>-to-<出力形式>_convert-log.yaml`（例：`2026/0923-140512_json-to-yaml_convert-log.yaml`。時刻はローカル時刻）。**一覧はこのファイル名から作り**、中身は開きません
- **件数の上限**（既定 500）を超えたら古いものから消します。**1 件の本文の上限**（既定 1 MiB）を超える入力・結果は、本文を記録しません（その入力を含むログは再実行できません）。ログの名前に合わないファイルは、消しません

### 16-3. ログファイルの中身

**YAML の複数文書（`---` 区切り）で、人がそのまま読めます。** 4 つの文書が並びます。

```yaml
# ----- summary -----
yaqpy_log: 1
yaqpy_version: 0.7.0
timestamp: "2026-09-23T14:05:12+09:00"
input:
  format: json
  selected: auto
  files:
    - name: sample.json
      path: D:\work\sample.json
      bytes: 77
      edited: false
output:
  format: yaml
  selected: yaml
  indent: 2
  bytes: 22
eval_all: false
document_count: 1
elapsed_ms: 1.8
security:
  allow_env: false
  allow_file: false
rerunnable: true
---
# ----- expression -----
# expression
expression: ".items[]\n| select(.enabled)"
---
# ----- original (input) -----
files:
  - name: sample.json
    format: json
    documents:
      - items:
          - name: a
            enabled: true
          - name: b
            enabled: false
---
# ----- result (output) -----
format: yaml
documents:
  - name: a
    enabled: true
```

- 入力と結果は**YAML に変換して**書きます（元の形式の原文ではありません）。式は `"…"` の**1 行**で、改行は `\n` です（`#` のコメントも失われません）
- **書く前に読み戻して確かめます。** 元の形式に戻せない項目（例：`.items[].name` の結果 `a`＋改行＋`b` は、YAML として読み直すと 1 つの値になってしまいます。同じキーが 2 度出てくる結果も）は、`documents` の代わりに**原文（`text:`）**を書き、概要の `representation` に残します
- `yaqpy` 自身でも読めます：`yaqpy 'select(document_index == 1) | .expression' ログ.yaml`（式）、`yaqpy 'select(document_index == 2) | .files[0].documents[0]' ログ.yaml`（入力データ）

### 16-4. ログ画面

下の **`[ログ]`** タブ（4 つ目）で開きます。

- **一覧**（左）：新しい順に「日時」「入力形式 → 出力形式（ファイルの大きさ）」。**タブを開くたびに読み直します**（`[🔄]` でも）
- **絞り込み**（上）：日時・形式は、その場で。**式**と**入力ファイル名**は、ファイルの**先頭部分だけ**（最大 64 KiB）を読んで照合します（一度読んだら覚えています）。空白で区切った語は、すべてに一致するものだけを残します（大文字小文字は区別しません）
- **詳細**（右）：一覧で選んだときに初めてファイルを読み、入力ファイル名・形式・式と、ログの全文（読み取り専用）を出します。長いログは[設定](#9-設定画面)の表示行数で丸めます。壊れているログは全文だけを出し、再実行はできません
- `[📂]` 保存先のフォルダを開く／`[🗑]` すべて削除（件数を出して確認します。ログの名前に合うファイルだけを消します）
- `[削除]` 選んだ 1 件を、確認のうえで消します

#### 再実行

`[再実行]` は、ログの内容を **Main 画面へ戻して、すぐ実行**します。

- 入力は、記録した**元の形式に変換し直して**（整形は yaqpy の出力になります。データの中身は同じ）、**新しい文書として追加**します。開いている文書は閉じません。文書の名前は記録した名前で、元のファイルは読み直しません
- **式・入力形式（実際に使った形式）・出力形式（画面で選んでいた値）・インデント**も戻します
- **`eval_all` で記録したログ**（複数の文書をまとめて評価したもの）だけは、開いている文書をすべて閉じて、ログの入力に**置き換えて**再実行します（確認が出ます。ログに無い文書を巻き込まないためです）
- **再実行した回は記録しません**（あとで `[実行]` を押せば、通常どおり記録されます）
- 本文を省いたログ・未知の版のログ・壊れたログは、`[再実行]` が押せません（式の保存はできます）

#### 式を `.yaqpy` に保存

`[式を .yaqpy に保存]` で、選んだログの式を [8-4](#8-4-式をファイルに保存読み込みyaqpy) と同じ形式で保存します（ファイル名の初期値は `入力名.yaqpy`）。

---
[toTop](#toreadme)
